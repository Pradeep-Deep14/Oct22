Tuple=(64,65,'A')
Max=max(Tuple)
print(Max)



#It would throw an exception since the max() function can’t compare strings to ints. If it could, the answer would be 65 because the ASCII value 65 corresponds to “A”.

#You can get the ASCII value of a character in python with the ord() function!